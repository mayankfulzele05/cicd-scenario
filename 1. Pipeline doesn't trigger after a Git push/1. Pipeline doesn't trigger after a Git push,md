name: Production Release Workflow
# ❌ CONFIGURATION FLUFF & TRAPS INSIDE THIS TRIGGER MATRIX
on:
  push:
    branches:
      - main
      - master
      - 'release/**'
    paths:
      - 'src/backend/**'
      - 'config/*.json'

jobs:
  validate-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Initialize Verify
        run: echo "Executing critical tests..."




